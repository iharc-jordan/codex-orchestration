# Membership-only negative control

This issue is a disposable test of explicit enrollment. Project membership does
not authorize execution. Keep it READY and never enroll it. It must not acquire
an attempt, worker session, or writable assignment checkout during the managed
workflow. There are no permitted implementation actions.
